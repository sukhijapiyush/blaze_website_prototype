$(document).ready(function() {
	pa.init();
	
	$('.drawer-menu a').click(function(e) {
		if ($(window).width() <= 1280) {
			$('.drawer').drawer('close');
		}
		var href = $(this).attr('href');
		pa.scrollToElement(href);
		
		e.preventDefault();
		return false;
	});
});